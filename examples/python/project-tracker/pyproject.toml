[project]
name = "project-tracker"
version = "0.1.0"
description = "Session resume example for Kimi Agent SDK"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["kimi-agent-sdk"]

[tool.uv.sources]
kimi-agent-sdk = { path = "../../../python" }
